import { WarningTextDirective } from './warning-text.directive';

describe('WarningTextDirective', () => {
  it('should create an instance', () => {
    const directive = new WarningTextDirective();
    expect(directive).toBeTruthy();
  });
});
